# アプリ内CSS（.sidebar-card の白背景カード等）はライトテーマ前提のため、
# 利用者のOS/ブラウザ設定に関わらずテーマをライトに固定する
[theme]
base = "light"

# 本番公開時のエラー情報保護：例外のトレースバック（内部パス・コード構造）を
# 利用者の画面に表示しない。エラー本文はサーバー側のログで確認する。
# 開発中に画面でトレースバックを見たい場合は "full" に変更する。
[client]
showErrorDetails = "none"
