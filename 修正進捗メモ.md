# コードレビュー修正の進捗メモ（2026-07-20）

## ✅ 全項目修正完了（2026-07-20・コミット未実施）

下記「未着手」セクションを含む全項目の修正が完了した。
構文チェック（py_compile）、venv での import 確認、report_gen の分岐テスト、
バッジ判定ロジックのテスト、e-Gov エラー分岐のモックテストまで実施済み。
残作業は動作確認（Streamlit 起動）とコミットのみ。

---

以下は修正時の作業記録。

## レビューで確定した指摘一覧（重要度順）

### 🔴 重大
1. `app.py:1095` — `status.write(str(e))` が生の例外メッセージをUIに表示（エラー詳細漏洩）
2. `app.py:2213` — `if "あり" in str(val)` が「ありません」にも部分一致し、⚠️バッジ誤表示
3. `backend/workflow.py` 網羅性チェック — LLM呼び出し失敗時に `None` → 「✅全論点カバー」と偽表示
4. `backend/tools/egov.py` — e-Gov API失敗を握りつぶし空リスト返却（「該当なし」と「未確認」の混同）

### 🟡 中程度（今回のスコープ内）
5. `backend/workflow.py:691`（旧行番号）— `action.done or not action.query` で空クエリを「調査完了」扱い
6. `backend/workflow.py` `_ground_relevant_articles` / `_prefetch_article_excerpts` — 条文選定LLM失敗を無警告で握りつぶし

### 🟡🟢 スコープ外（今回は修正しない・将来課題）
- case_memory.py / internal_docs.py の読み込みロック無し・非アトミック書き込み
- web_search.py のタイムアウト未設定・429以外リトライなし
- workflow.py:881-921 の上限切り捨てで WEB_MIN_KEEP 保証が破れるケース
- report_gen.py の case_id / law_id の `_esc()` 未適用
- doc_export.py の fitz doc 未クローズ・Windows固定フォントパス
- egov.py の XMLキャッシュ無制限
- workflow.py:302-309 「未記入」部分一致
- app.py の `render_step_banner` デッドコード・PHASE_INDEX デフォルト不一致（1 vs 0）
- app.py:1908 検索候補HTMLの非サンドボックス埋め込み

## ✅ 修正済み（コミット未実施）

### backend/tools/egov.py — 完了
- `import logging` + `logger` 追加
- `EgovAPIError` 例外クラスを新設（「該当法令0件」と「APIエラー」の区別用）
- `_search_by_title` / `_search_by_fulltext`: `print` → `logger.warning`、空リスト返却 → `EgovAPIError` 送出
- `search_laws_by_keyword`: 内部 `_safe()` で各検索のエラーを収集。部分成功なら結果を返し、
  **全滅かつエラーありの場合のみ `EgovAPIError` を送出**。docstring位置の不備も修正

### backend/workflow.py — 途中（下記まで完了）
- `import logging` + `logger = logging.getLogger(__name__)` 追加
- `EgovAPIError` をインポートに追加
- `search_node` に `egov_api_errors = 0` カウンタ追加
- シード検索ループ（旧558行）: try/except EgovAPIError、失敗時は
  「⚠️ [e-Gov] 「kw」→ APIエラーで取得失敗（0件は「該当なし」ではなく未確認）」をログ・進捗に記録
- Agenticループ空クエリ（旧691行）: `action.done` と `not action.query` を分離。
  空クエリは「調査完了」ではなく skip 扱い（consecutive_skips 加算、3回で終了）
- Agentic e-Gov追加検索（旧735行）: try/except EgovAPIError + ⚠️エントリ
- 網羅性チェック: `coverage_check_failed` フラグ新設。
  - `_run_coverage_check` 失敗時に `logger.warning(exc_info=True)`
  - 初回チェック失敗 → ⚠️警告を emit / search_log / progress_messages に記録、✅を出さない
  - 再チェック失敗 → 補完前の未カバー論点を安全側で `uncovered_issues` に残して⚠️警告
  - `elif check is not None:` で「✅全論点カバー」はチェック成功時のみ表示

## ❌ 未着手（再開時にやること）→ ✅ すべて実施済み

1. **workflow.py 続き**:
   - 網羅性補完のe-Gov検索（旧820行 `laws = search_laws_by_keyword(q, max_results=5)`）にも
     try/except EgovAPIError + egov_api_errors カウント + ⚠️エントリを追加
   - `search_node` の調査完了サマリー直前（`summary_msg` 付近）に、`egov_api_errors > 0` なら
     集約警告「⚠️ e-Gov APIエラーが N 件発生。該当検索は未確認のため確認漏れの可能性あり」を
     progress_messages + emit で出す
   - `search_node` の return dict に `"coverage_check_failed": coverage_check_failed` を追加
   - `results_review_node` の interrupt payload（`uncovered_issues` の隣）に
     `"coverage_check_failed": state.get("coverage_check_failed", False)` を追加
   - `report_node` の `generate_html_report(...)` 呼び出しに
     `coverage_check_failed=state.get("coverage_check_failed", False)` を追加
   - 確認漏れ防止チェック（旧1266行）: `except Exception` → `except EgovAPIError` にして
     emit で警告（挙動は hits=[] のまま）
   - law_id補完検索（旧1371行）: try/except EgovAPIError で包む（失敗時は law_id 未解決のまま
     ＝「条番号確認中」表示になる。logger.warning を出す）
   - `_ground_relevant_articles`（旧994行 `except Exception: law["relevant_articles"] = []`）:
     logger.warning + emit「⚠️ 「law_name」の条番号照合AI呼び出しに失敗。条番号は未確定と表示」
   - `_prefetch_article_excerpts`（旧1065行 `except Exception: continue`、旧1077行 `texts = {}`）:
     同様に logger.warning + emit で警告を出してから continue / 空dict

2. **backend/state.py**: `AppState` に `coverage_check_failed: bool` を追加
   （`uncovered_issues` の近く、コメント付き）

3. **backend/report_gen.py**:
   - `generate_html_report` に引数 `coverage_check_failed: bool = False` を追加
   - `_build_coverage_check(issues, uncovered, coverage, check_failed=False)` に拡張:
     - `check_failed=True` のときは緑の「✅ チェック結果 OK」を絶対に出さず、
       琥珀色の警告ボックス「⚠️ 網羅性チェックを完了できませんでした（AI呼び出しエラー）。
       論点カバー状況は自動確認されていません。手動で確認してください」を表示
     - check_failed 時は論点別✅/⚠️詳細テーブルは出さない（✅カバー表示が誤解を招くため）。
       uncovered があればそのリストは表示する
   - 冒頭の early-return 条件 `if not issues and not uncovered:` に `and not check_failed` を追加

4. **app.py**:
   - **1095行付近**: `status.write(str(e))` を削除（logger には traceback 記録済み。
     直後の add_display の汎用メッセージだけ残す）
   - **2213行付近のバッジ判定**を並べ替え:
     ```python
     sval = str(val)
     if re.search(r"あり(?!ません)", sval):
         badge = "⚠️"
     elif sval.startswith("なし") or "ありません" in sval:
         badge = "✅"
     elif sval in ("不明", "未定", "確認中"):
         badge = "❓"
     else:
         badge = "📌"
     ```
     （`re` は import 済み）
   - **2244行**: `st.error(f"登録に失敗しました: {flash['error']}")` →
     生の例外を出さず汎用文言「登録に失敗しました。時間をおいて再度お試しください。」
   - **2303行**: `st.session_state.ingest_result = {"error": str(e)}` → `{"error": True}`
   - **618行付近**（QA用 results dict）: `"coverage_check_failed": get_state_value(tid, "coverage_check_failed") or False,` を追加
   - **1538-1548行付近**（結果確認画面）:
     `coverage_failed = idata.get("coverage_check_failed") or False` を取り、
     `if issues_list and not uncovered:` → `if issues_list and not uncovered and not coverage_failed:`
     に変更。`coverage_failed` 時は st.warning で「網羅性チェック未実行・手動確認要」を表示

5. **動作確認**: `python -c "import app"` 相当の構文チェック
   （`py_compile` で app.py / workflow.py / report_gen.py / egov.py / state.py）と、
   可能なら Streamlit 起動確認

## 備考
- ここまでの変更はすべて未コミット。egov.py と workflow.py が中途半端な整合状態
  **ではない**点に注意：egov.py が例外を投げるようになった一方、workflow.py の
  旧820行（網羅性補完）と旧1371行（law_id補完）は try/except 未追加のため、
  **現状のままだと e-Gov API 障害時にそこで例外が上に抜ける**。
  再開時は最優先で「未着手 1.」の最初の2項目（820行・1371行の try/except）を先に入れること。
- 旧1266行は既存の `except Exception` があるので現状でも例外は漏れない。
